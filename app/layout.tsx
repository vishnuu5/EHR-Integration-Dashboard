import type React from "react";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";
import { Toaster } from "@/components/ui/sonner";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "EHR Integration Dashboard",
  description:
    "Comprehensive Electronic Health Records Integration Dashboard with CRUD operations for healthcare workflows",
  keywords:
    "EHR, Electronic Health Records, Healthcare, Dashboard, FHIR, DrChrono, Epic",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className} suppressHydrationWarning>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
